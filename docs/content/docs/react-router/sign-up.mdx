---
title: Sign Up
description: A registration card with username, email, password, and confirm password fields powered by @aura-stack/react-router.
---

The `SignUp` block wraps `useSignUp` from `@aura-stack/react-router` and validates against the same schema your `auth.ts` is configured with.

<BlockPreview
  name="sign-up-1-react-router"
  description="Sign Up 1"
  installCommand="npx shadcn add https://aura-ui.vercel.app/r/sign-up-react-router"
/>
