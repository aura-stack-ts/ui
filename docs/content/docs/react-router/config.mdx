---
title: Config
description: The authentication configuration for React Router.
---

## Installation

Run the following command to install the `auth.ts` and `auth-client.ts` files:

<Tabs items={["CLI"]}>

<Tab value="CLI">

```npm
npx shadcn add https://aura-ui.vercel.app/r/react-router-setup
```

</Tab>

</Tabs>
